import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nigerconnect.app',
  appName: 'NigerConnect',
  webDir: 'dist/client/browser'
};

export default config;
